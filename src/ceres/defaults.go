package ceres

/*
func (c *CERES) DefaultStartUp() {
	if c.root != nil {
		panic("Root set.")
	}

	root, _ := c.addET("entity", nil, group{})

	thing, _ := c.addET("thing", root, group{name: "NOUN_PHYS"})
	attribute, _ := c.addET("attribute", root, group{name: "ADJ"})
	concept, _ := c.addET("concept", root, group{name: "NOUN_IDEA"})
	being, _ := c.addET("being", thing, group{})

	var rules = []rule{}

	c.grammar.rules = rules
}*/
